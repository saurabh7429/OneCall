<?xml version="1.0" encoding="utf-8"?>
<!-- Secondary Dashboard: Added Reconnect + Go to Setup buttons, improved layout -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_dark"
    android:padding="20dp">

    <!-- App Bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="20dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/app_name"
            android:textSize="26sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary" />

        <ImageView
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:src="@drawable/ic_bluetooth"
            android:alpha="0.6" />

    </LinearLayout>

    <!-- Connection Status Card -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardBackgroundColor="@color/bg_card"
        app:cardCornerRadius="16dp"
        android:layout_marginBottom="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp"
            android:gravity="center">

            <ImageView
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:src="@drawable/ic_bluetooth"
                android:layout_marginBottom="12dp" />

            <TextView
                android:id="@+id/tv_connected_to"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Not Connected"
                android:textSize="17sp"
                android:textStyle="bold"
                android:textColor="@color/text_primary"
                android:gravity="center" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginTop="8dp">

                <View
                    android:id="@+id/status_dot"
                    android:layout_width="10dp"
                    android:layout_height="10dp"
                    android:background="@drawable/ic_disconnected"
                    android:layout_marginEnd="8dp" />

                <TextView
                    android:id="@+id/tv_connection_state"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/state_disconnected"
                    android:textSize="14sp"
                    android:textColor="@color/text_secondary" />

            </LinearLayout>

        </LinearLayout>

    </androidx.cardview.widget.CardView>

    <!-- Reconnect Button — shown when disconnected -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_reconnect"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:text="🔄  Reconnect to Main Device"
        style="@style/Widget.OneCall.Button.Primary"
        android:layout_marginBottom="10dp"
        android:visibility="gone" />

    <!-- Go to Setup — to scan/pair again -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_go_setup"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="📡  Scan for Main Device"
        style="@style/Widget.OneCall.Button.Secondary"
        android:layout_marginBottom="10dp"
        android:visibility="gone" />

    <!-- Open Dialpad -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_open_dialpad"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:text="Open Dialpad"
        style="@style/Widget.OneCall.Button.Secondary" />

</LinearLayout>
